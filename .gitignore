# OS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Tooling state (oh-my-claudecode and similar local agents)
.omc/
.claude-mem/

# Ada object/library/coverage build artifacts
runtime/obj/
runtime/lib/
runtime/tests/obj/
runtime/bench/obj/
runtime/config/
runtime/coverage.lcov
*.gcda
*.gcno
*.lexch
*.bexch
*.adb.stdout
*.adb.stderr
*.ads.stdout
*.ads.stderr

# Alire transient state (alire.toml and alire/alire.lock are committed;
# the rest is per-checkout local state).
runtime/alire/build_hash_inputs
runtime/alire/cache/
runtime/alire/flags/
runtime/alire/settings.toml
runtime/alire/tmp/

# Go build artifacts
compiler/bin/
compiler/coverage.out

# Aggregated coverage outputs (generated by `make coverage`)
coverage/

# Example binaries (Mach-O / ELF; list known binaries explicitly)
examples/hello/hello
examples/collections/collections
