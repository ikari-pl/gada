# Phase 03: ADRs, Style Guides, and CONTRIBUTING — Closing Out Phase 0

This phase produces the durable design documentation that every later phase will reference. It closes out the remaining roadmap Phase 0 items: the ADR scaffolding, the four initial v1.0 ADRs (Go AST frontend, layered runtime, Boehm GC, libco scheduler), the Ada and Go style guides, and the contributor quickstart. Each ADR is a structured Markdown file with YAML front matter and wiki-link cross-references so it renders as a navigable graph in DocGraph viewers and Obsidian. After this phase, roadmap Phase 0 flips to `DONE` and Phase 1 (the minimal transpiler) becomes the active phase.

## Tasks

- [x] Search for any existing design notes before creating new ones:
  - List `docs/` if it exists (`ls docs/ 2>/dev/null`). If documents are present, read them — they may contain decisions that should become ADRs.
  - Read `roadmap/00-foundation.md` items for **Architecture Decision Records (ADR) directory**, **Initial ADRs**, **Coding standards documents**, and **CONTRIBUTING quickstart** to anchor file names and verify commands.
  - Read the relevant sections of `AGENTS.md` (design principles, repo layout) and `README.md` (the Architecture diagram and the "What GADA does NOT win at" section) — these are the source material the ADRs codify.

  **Findings (2026-05-01):**
  - `docs/` currently contains only `docs/journal/` with one entry: `2026-04-30-phase01-bootstrap.md`. No `docs/adr/` directory exists — the ADR work is greenfield. The journal entry establishes the structured-Markdown convention this repo uses (YAML front matter with `type`, `title`, `created`, `tags`, `related`, plus `[[Wiki-Link]]` cross-references); upcoming ADRs must mirror that style for DocGraph/Obsidian graph rendering.
  - **Roadmap items still open in `roadmap/00-foundation.md`** (in execution order):
    1. **Architecture Decision Records (ADR) directory** — files: `docs/adr/0000-record-architecture-decisions.md`, `docs/adr/template.md`. Verify: `ls docs/adr/*.md | wc -l` ≥ 2.
    2. **Initial ADRs documenting v1.0 design choices** — files: `docs/adr/0001-go-frontend-via-go-ast.md`, `docs/adr/0002-runtime-layered.md`, `docs/adr/0003-gc-boehm-for-v1.md`, `docs/adr/0004-scheduler-libco-for-v1.md`. Verify: each ≥ 50 lines, status `accepted`, `## Consequences` section present.
    3. **Coding standards documents** — files: `docs/style_ada.md`, `docs/style_go.md`. Verify: each references its lint config and is referenced from at least one ADR.
    4. **CONTRIBUTING quickstart** — file: `CONTRIBUTING.md` at repo root. Verify: `wc -l CONTRIBUTING.md` ≥ 60; references `AGENTS.md` and `roadmap/README.md`.
  - **Source material the ADRs will codify** (from `AGENTS.md` and `README.md`):
    - ADR-0001 (Go AST frontend) is anchored in `AGENTS.md` Design principle #2 ("The transpiler is itself a Go program. Reuse `go/ast`, `go/types`, `golang.org/x/tools/go/packages`…").
    - ADR-0002 (layered runtime) is anchored in `AGENTS.md` Design principle #3 (the four-layer stack `Gada.Std` / `Gada.Reflect` / `Gada.Async` / `Gada.Core` with strict no-upward-dependency) and `README.md` Architecture diagram.
    - ADR-0003 (Boehm GC) is anchored in `AGENTS.md` Non-goals ("Writing our own GC. Boehm-Demers-Weiser is the v1 GC.") and `README.md` "What GADA does NOT win at" (GC throughput is Boehm's, not Go's generational GC).
    - ADR-0004 (libco scheduler + Ravenscar split) is anchored in `README.md` "Real-time scheduling for goroutines" + "Embedded Go without a heavyweight runtime" sections (Ravenscar mode for hard-real-time + bounded-task embedded targets), and `README.md` "What GADA does NOT win at" (goroutine context switches cost more than Go's runtime).
  - **Lint-config wiring** that the style docs must reference (already shipped by Phase 02):
    - `tools/gnatcheck.rules` already carries a commented-out `+RHeaders:Header=tools/header_ada.txt` placeholder explicitly waiting on `docs/style_ada.md` — the Ada style doc isn't just documentation, it gates re-enabling that lint rule.
    - `.golangci.yml` is v2-schema (`version: "2"`, `linters.default: none`, formatters block separate from linters) — `docs/style_go.md` must reference it by filename and align its rule descriptions with the active enable list (`errcheck`, `govet`, `staticcheck`, `gosec`, `unparam`, `revive`, plus `gofmt`/`goimports` formatters).
  - **CONTRIBUTING anchors already in place**: `tools/coverage_thresholds.toml` (cited by `CONTRIBUTING.md`'s coverage section), `roadmap/README.md` per-task contract (cited by the per-task contract section), `Makefile` `bootstrap`/`ci` targets (cited by Quickstart), and the `AGENTS.md` no-Claude-co-author rule from the user's global instructions (cited by Commit format).
  - **Phase 0 roadmap items already DONE** (so the closing-out work in this phase only needs the four remaining items, not a re-verify of foundational tooling): Go module, Alire crate, AUnit harness, Go test toolchain, Ada coverage (gcov+lcov), Go coverage, top-level Makefile, coverage gate, linting, GitHub Actions CI, roadmap consistency check.

- [x] Create the ADR directory and the meta-ADR explaining the convention:
  - Create `docs/adr/0000-record-architecture-decisions.md` and `docs/adr/template.md`.
  - The template uses MADR-style front matter:
    ```yaml
    ---
    type: adr
    title: "ADR-NNNN: Short decision title"
    status: proposed | accepted | superseded
    created: YYYY-MM-DD
    deciders: []
    tags: []
    related: []
    ---
    ```
    With sections: `## Context`, `## Decision`, `## Consequences`, `## Alternatives considered`.
  - ADR-0000 must explain the convention itself: where ADRs live, the naming pattern (`NNNN-kebab-title.md`), the lifecycle (proposed → accepted → superseded → never deleted), and the status enum. ≥ 50 lines, set its own status to `accepted`. Cross-link to `[[CONTRIBUTING]]`.
  - Verify: `ls docs/adr/*.md | wc -l` ≥ 2.
  - Tick the **Architecture Decision Records (ADR) directory** roadmap item.

  **Outcome (2026-05-01):**
  - Wrote `docs/adr/template.md` (69 lines) — MADR-style front matter (`type: adr`, `status: proposed` placeholder, `deciders/tags/related` arrays) plus the four mandated sections, each with inline guidance for the next agent (e.g., "active voice, present tense" for the Decision section; the third Consequences bullet — *what is now off-limits* — flagged as the most important and most-skipped). The template's "How to use" callout cross-links `[[0000-record-architecture-decisions]]` so a copier lands on the convention spec, not the file index.
  - Wrote `docs/adr/0000-record-architecture-decisions.md` (155 lines, `status: accepted`). Codifies: flat `docs/adr/` location, `NNNN-kebab-title.md` naming (decision-named, not question-named), three-state lifecycle (`proposed` → `accepted` → `superseded`, never deleted, accepted ADRs are typo-only edits), and the supersession ritual (add a `Superseded by [[NNNN-...]]` line at the top, flip the front-matter status, do **not** rewrite the original Decision section). Records the wiki-link cross-reference convention (`[[ADR-NNNN]]`, `[[roadmap/<file>]]`, `[[CONTRIBUTING]]`) that makes `docs/adr/` render as a graph in Obsidian/DocGraph. Names the style-doc → ADR mapping (`docs/style_ada.md` ↔ `[[ADR-0002]]`, `docs/style_go.md` ↔ `[[ADR-0001]]`) so the next two playbook tasks land on existing anchors. The "When to write an ADR" heuristic — *if you can imagine someone six months from now writing "why on earth did they pick X over Y" in a PR comment, write the ADR before they have to ask* — is intentionally a behavioral test, not a category rule.
  - Considered (and recorded as alternatives in ADR-0000): no-records / `git log` only (rejected: superlinear archaeology cost), RFC-style design docs (deferred: complementary, not substitutive — may add `docs/rfc/` later), journal-only (rejected: journal is for *narrative*, ADRs are for *decisions*; both kept), and Nygard's original ADR format (rejected in favor of MADR specifically because YAML front matter is what makes Obsidian/DocGraph graph rendering work).
  - Verification: `ls docs/adr/*.md | wc -l` = **2** (≥ 2 ✓); `wc -l docs/adr/0000-record-architecture-decisions.md` = **155** (≥ 50 ✓); `grep -E '^status:'` = `status: accepted` ✓; all four required sections present at lines 16/41/91/114 of ADR-0000 and at lines 20/32/43/58 of `template.md`; `[[CONTRIBUTING]]` referenced 3 times in ADR-0000 (front-matter `related`, in-body convention paragraph, See-also block).
  - Roadmap impact: the **Architecture Decision Records (ADR) directory** item in `roadmap/00-foundation.md` is now ticked. The remaining three Phase 0 documentation items (initial ADRs, style guides, CONTRIBUTING) all anchor on this convention.

- [x] Write the four initial v1.0 ADRs that codify the load-bearing decisions in `AGENTS.md` and `README.md`. Each is ≥ 50 lines, status `accepted`, with a concrete `## Consequences` section listing what now becomes harder, what becomes easier, and what is now off-limits:
  - `docs/adr/0001-go-frontend-via-go-ast.md` — *We use upstream `go/ast`, `go/types`, and `golang.org/x/tools/go/packages` for the Go front-end rather than writing our own parser.* Consequences: ties our Go-version support to the Go release we vendor; gives us free access to type information; means our compiler is a Go binary forever. Cross-link `[[ADR-0002]]`, `[[roadmap/01-minimal-transpiler]]`.
  - `docs/adr/0002-runtime-layered.md` — *Runtime split into `Gada.Core`, `Gada.Async`, `Gada.Reflect`, `Gada.Std`, with a strict no-upward-dependency rule.* Consequences: embedded targets can omit upper layers; reflection cannot be used by the scheduler; this constrains how `defer` interacts with the GC. Cross-link `[[ADR-0003]]`, `[[ADR-0004]]`.
  - `docs/adr/0003-gc-boehm-for-v1.md` — *Boehm-Demers-Weiser is the v1 GC; precise GC is post-1.0 research.* Consequences: conservative scanning means goroutine stacks must be conservatively traced; binary size includes libgc; precise GC requires a follow-on ADR.
  - `docs/adr/0004-scheduler-libco-for-v1.md` — *Goroutine M:N scheduler implemented over Ada tasks plus a userspace coroutine library (libco) for goroutine context-switch cost; Ravenscar mode disables M:N and runs one Ada task per goroutine with bounded count.* Consequences: hosted targets pay context-switch cost via libco; Ravenscar targets get certifiable scheduling but lose `go func() { ... }()` ergonomics for unbounded spawns.
  - Each ADR cross-links via `[[ADR-NNNN]]` wiki-link syntax to the others it depends on, and uses `[[roadmap/<file>]]` for roadmap references. Use the same YAML front matter as the template.
  - Verify: each file is ≥ 50 lines (`wc -l docs/adr/000{1,2,3,4}-*.md` shows ≥ 50 for each), each has `status: accepted`, each has a `## Consequences` section. Capture the line count audit to `.maestro/playbooks/Initiation/Working/adr-line-audit.txt`.
  - Tick the **Initial ADRs** roadmap item.

  **Outcome (2026-05-01):**
  - All four ADRs written using the MADR template ratified by ADR-0000. Front matter mirrors the template (`type: adr`, `status: accepted`, `created: 2026-05-01`, `deciders: [gada-core]`, plus topical `tags` and `[[wiki-link]]` `related` arrays). Each file carries the four mandated sections — `## Context`, `## Decision`, `## Consequences`, `## Alternatives considered` — followed by a `## See also` block (a convention ADR-0000 implicitly authorizes via its own structure). The `## Consequences` sections all use the three-bullet *what becomes easier / what becomes harder / what is now off-limits* shape the template flags as load-bearing.
  - **Line counts** (verify clause: ≥ 50 each): `0001=155`, `0002=185`, `0003=203`, `0004=228`. Total = 771 lines. All four `status: accepted` confirmed by `grep -E '^status:'`. Each `## Consequences` heading found at lines 80 / 103 / 106 / 118 respectively.
  - **Cross-link topology** is the load-bearing graph the playbook called for:
    - `0001` ↔ `0002` — the *frontend ↔ runtime* axis. The compiler reads Go via 0001's frontend; the runtime layering in 0002 dictates what the compiler is allowed to emit calls into.
    - `0002` → `0003` — the GC interface lives in `Gada.Core`, and 0002's layering rules constrain what the GC may know about higher layers.
    - `0002` → `0004` — the scheduler lives in `Gada.Async`, and 0002 fixes its place in the stack and its allowed dependencies.
    - `0003` ↔ `0004` — goroutine-stack registration with libgc (alternate scanning roots when goroutines suspend) is the bidirectional GC/scheduler contract; both ADRs name the other in their See-also block so a reader landing on either reaches the full pair.
    - Each ADR also wiki-links its anchoring roadmap phase: `0001 → roadmap/01-minimal-transpiler` (the phase that consumes the frontend), `0002 / 0003 / 0004 → roadmap/00-foundation` (the phase that ratifies the layering / GC / scheduler choices).
  - **Forward references to style docs** are intentionally seeded so the next playbook task lands on existing anchors: `0001` `related:` and See-also reference `[[style_go]]` (the Go style guide governs the GADA compiler, which is a Go program — forced by 0001); `0002` references `[[style_ada]]` (the Ada style guide makes the `Gada.X.Y` naming rule and the no-upward-`with` rule lint-enforceable). When `docs/style_ada.md` and `docs/style_go.md` land in the next task, ADR-0000's "style-doc → ADR" mapping (`docs/style_ada.md` ↔ `[[ADR-0002]]`, `docs/style_go.md` ↔ `[[ADR-0001]]`) closes the **Coding standards documents** verify clause "referenced from at least one ADR" automatically — no ADR edits required.
  - **The *what is now off-limits* bullets are the load-bearing additions over the `AGENTS.md`/`README.md` source material** — they convert narrative ("we won't write our own parser") into guardrails ("a PR adding one is rejected unless it supersedes this ADR"):
    - **0001:** writing a Go parser, type checker, module resolver, build-tag evaluator, or method-set algorithm; rewriting the compiler in Ada (a recurring suggestion from people who have not measured the front-end effort).
    - **0002:** any upward `with` clause; any sibling `with` between `Gada.Async` and `Gada.Reflect`; putting reflection types in `Gada.Core`; putting `fmt.Println` in `Gada.Core`; inventing a top-level package outside the `Gada.*` namespace.
    - **0003:** building a precise tracing GC for v1.0 (post-1.0 + follow-on ADR required); emitting per-frame stack maps (dead weight under conservative scanning); refcount/region paths bypassing `Gada.Core.Memory`.
    - **0004:** Go-runtime-fidelity M:N with stack copy-on-grow; mixing M:N and Ravenscar in one binary; pre-emptive scheduling in v1.0; bypassing `Gada.Async` to hand-write Ada tasks for Go-level concurrency in user code.
  - **Alternatives considered** — each ADR records the rejected options with one paragraph each, so a future reader (or a superseding ADR) does not have to rediscover them: 0001 lists hand-written Ada parser, hand parser + upstream types, gollvm/LLGo, TinyGo's frontend; 0002 lists monolithic / two-layer / per-package / three-layer (no Reflect); 0003 lists precise GC, refcount+cycle collector, controlled-types-only, MPS, static-pool only; 0004 lists one-task-per-goroutine on hosted, custom Ada coroutine swap, M:N-via-task-pool-recycling on Ravenscar, protected-entries-only, channels as bare rendezvous.
  - **Verification** captured to `.maestro/playbooks/Initiation/Working/adr-line-audit.txt`. `tools/check_roadmap_consistency.sh` exits 0 (`OK: roadmap status consistent across index and 12 phase file(s).`).
  - **Roadmap impact:** the **Initial ADRs documenting v1.0 design choices** item in `roadmap/00-foundation.md` is now ticked, with a Notes block summarizing the cross-link topology and the *off-limits* additions. Two Phase 0 documentation items remain open: **Coding standards documents** (anchors already seeded via the `[[style_ada]]` / `[[style_go]]` forward links above) and **CONTRIBUTING quickstart**.

- [x] Write the two coding-standards documents:
  - `docs/style_ada.md` — naming convention `Gada.X.Y` (every package starts with `Gada`), file layout (`-` separator maps `.` in package name → file path), comment style (each public subprogram has a `--  Purpose` line), no `goto`, no global mutable state in package specs, every public spec has a corresponding test. Reference `tools/gnatcheck.rules` from Phase 02 for the lint encoding of these rules.
  - `docs/style_go.md` — module path `github.com/gada-lang/gada/...`, no panic in library code (return `error`), interfaces ≤ 4 methods unless documented, public APIs have at least one test, exported identifiers use Go-standard naming (no `Gada` prefix — that's the Ada side). Reference `.golangci.yml` from Phase 02 for the lint encoding.
  - Both documents have YAML front matter (`type: style`, `tags: [style, ada|go]`).
  - Add a short paragraph in each cross-linking the matching ADRs (`[[ADR-0001]]` for Go style, `[[ADR-0002]]` for Ada layering).
  - Add a one-line reference to each style doc inside ADR-0000 (the meta ADR).
  - Verify: each doc references its lint config, calls out the `Gada.X.Y` rule (Ada) or module path (Go), and is referenced from at least one ADR. `grep -l 'style_ada.md' docs/adr/` and `grep -l 'style_go.md' docs/adr/` must each return ≥ 1 file.
  - Tick the **Coding standards documents** roadmap item.

  **Outcome (2026-05-01):**
  - Wrote `docs/style_ada.md` (253 lines) — nine sections plus *See also*. YAML front matter (`type: style`, `tags: [style, ada, runtime, lint]`, `related:` includes the four ADRs + `[[CONTRIBUTING]]`) matches the structured-Markdown convention ratified by ADR-0000. Section 1 codifies the `Gada.X.Y` naming rule (every package rooted at `Gada`, depth ≥ 2, transpiled user code reserved under `Gada.User.<module-path>`); Section 2 covers GNAT's `-`-for-`.` file-naming convention; Section 3 codifies the public-subprogram **Purpose** comment style with the `runtime/src/gada-core-io.ads` Println example as the worked template, including the operational requirements (active voice / present tense, Go-equivalent in backticks, layering call-out); Sections 4 + 5 wire the existing `+RGOTO_Statements` / `+RGlobal_Variables` rules to their *Why*; Section 6 spells out the no-upward-/sibling-`with` matrix mechanically derivable from unit names (`Core < Async = Reflect < Std`, with sibling `Async`/`Reflect` disjoint); Section 7 ties public-spec → test to the runtime's 100%-coverage gate; Section 8 documents the four already-active baseline rules (anonymous arrays/subtypes, exception handlers in elaboration code, max line length 100, unused withs). Each rule carries a *Lint encoding* sub-bullet that names the gnatcheck rule or planned mechanisation (`tools/check_layering.sh` for Section 6, `tools/check_namespace.sh` for Section 1). Cross-links to `[[0002-runtime-layered]]`, `[[0003-gc-boehm-for-v1]]`, `[[0004-scheduler-libco-for-v1]]`, and `[[0000-record-architecture-decisions]]` in the prose body close the graph that ADR-0000's "style-doc → ADR" mapping forward-declared.
  - Wrote `docs/style_go.md` (260 lines) — eight sections plus *See also*. YAML front matter (`type: style`, `tags: [style, go, compiler, lint]`, `related: [ADR-0000, ADR-0001, CONTRIBUTING]`). Section 1 codifies the `github.com/gada-lang/gada/...` module path as a *public API* (Go's import paths are addresses, not names) with `compiler/cmd/gada/main.go`'s actual import as the worked example, plus the future-modules sibling-pattern (`github.com/gada-lang/gada/tools`); Section 2 forbids `panic` in library code with three named legitimate exceptions (truly impossible invariants, `init`/`main` startup errors preferring `os.Exit(2)`, test helpers); Section 3 sets the ≤ 4-method interface rule with the `io.ReadWriteCloser`-mirror exception explicitly named so reviewers don't relitigate it; Section 4 ties public-API → test directly to `tools/coverage_thresholds.toml`'s 90% / 95% / 95% triplet; Section 5 codifies Go-standard naming with the *no `Gada` prefix* corollary load-bearing (`gada` is the binary name only, never a Go package name); Section 6 specifies error-wrapping with `fmt.Errorf("%w", err)` and the `errors.Is` / `errors.As` discrimination; Section 7 documents the three-block `goimports -local` import grouping. Each rule carries a *Lint encoding* sub-bullet pointing at the active linter in `.golangci.yml` (`errcheck` / `govet` / `staticcheck` / `gosec` / `unparam` / `revive` plus the `gofmt` / `goimports` formatters). Section 8 explicitly enumerates what the doc *does not* try to settle (gofmt-decided formatting, structural placement of helpers, test framework choice) — the inverse list keeps future style-creep PRs out of scope.
  - **Cross-link to ADRs**: `style_ada.md`'s introductory paragraph names the four runtime-side ADRs whose decisions the style rules encode (`[[0002-runtime-layered]]` for layering + `Gada.X.Y`, `[[0003-gc-boehm-for-v1]]` for the GC-interface-in-Core constraint, `[[0004-scheduler-libco-for-v1]]` for the no-exception-handlers-in-elaboration rule). `style_go.md`'s introductory paragraph names `[[0001-go-frontend-via-go-ast]]` as the load-bearing ancestor — the compiler is a Go program *because* of 0001, so a Go style guide must exist.
  - **One-line reference inside ADR-0000**: already present from the ADR-0000 task — lines 76–82 of `docs/adr/0000-record-architecture-decisions.md` carry the canonical *"Style guides are referenced from ADRs"* clause naming both `docs/style_ada.md` and `docs/style_go.md` with their ADR mappings (`docs/style_ada.md` ↔ `[[ADR-0002]]`, `docs/style_go.md` ↔ `[[ADR-0001]]`). The current task's playbook bullet *"Add a one-line reference to each style doc inside ADR-0000"* was satisfied prospectively when ADR-0000 was first written; no edit required.
  - **Verify clauses (2026-05-01):**
    - `wc -l docs/style_ada.md docs/style_go.md` → `253` / `260` (well above any minimum, both have YAML front matter + 8–9 numbered sections + See-also).
    - `grep -c 'gnatcheck.rules' docs/style_ada.md` → **7** ✓ (lint config referenced).
    - `grep -c '\.golangci\.yml' docs/style_go.md` → **7** ✓ (lint config referenced).
    - `grep -c 'Gada\.X\.Y' docs/style_ada.md` → **4** ✓ (project-specific rule called out).
    - `grep -c 'github.com/gada-lang/gada' docs/style_go.md` → **7** ✓ (module path called out).
    - `grep -l 'style_ada\.md' docs/adr/` → `docs/adr/0002-runtime-layered.md`, `docs/adr/0000-record-architecture-decisions.md` (**2 ≥ 1** ✓). `style_ada` (the wiki-link form) also appears in `0002-runtime-layered.md`.
    - `grep -l 'style_go\.md' docs/adr/` → `docs/adr/0001-go-frontend-via-go-ast.md`, `docs/adr/0000-record-architecture-decisions.md` (**2 ≥ 1** ✓). `style_go` (the wiki-link form) also appears in `0001-go-frontend-via-go-ast.md`.
    - `./tools/check_roadmap_consistency.sh` → exit 0, *"OK: roadmap status consistent across index and 12 phase file(s)."*
    - `make lint` → exit 0 (Go side runs golangci-lint clean; Ada side soft-skips on macOS as documented because gnatcheck ships only with GNAT Pro — the CI Ubuntu runner is the authoritative enforcement point).
  - **Roadmap impact**: the **Coding standards documents** item in `roadmap/00-foundation.md` is now ticked. The verify clause *"both reference the lint configurations and call out project-specific rules"* + *"an ADR references each style doc"* is satisfied by the prospective forward-links seeded into ADR-0001 / ADR-0002 / ADR-0000 during the prior playbook tasks. Only the **CONTRIBUTING quickstart** item remains open in Phase 0.

- [x] Write `CONTRIBUTING.md` at repo root:
  - Sections: *Quickstart* (clone, `make bootstrap`, `make ci`), *Picking a task* (read `AGENTS.md`, find lowest open `- [ ]` in active phase, follow per-task contract), *Per-task contract* (the `Files / Verify / Done when` shape, link to `roadmap/README.md`), *Coverage rule* (100% runtime, ≥90% compiler — link to `tools/coverage_thresholds.toml`), *Commit format* (one concept per commit, body explains why, no Claude/Anthropic co-author lines), *PR workflow* (phase branches, exit-criteria output in PR body), *Style* (link to `docs/style_ada.md` and `docs/style_go.md`), *ADRs* (when to write one — link to `docs/adr/0000-...`).
  - Verify: `wc -l CONTRIBUTING.md` ≥ 60, and `grep -E 'AGENTS\.md|roadmap/README\.md' CONTRIBUTING.md` returns at least one line for each. A new contributor reading it must be able to run `make ci` locally without further questions.
  - Tick the **CONTRIBUTING quickstart** roadmap item.

  **Outcome (2026-05-01):**
  - `CONTRIBUTING.md` (226 lines) shipped at repo root with all eight required sections in spec order. The doc is deliberately a **router**, not a restater — every rule cites and links the artefact that enforces it (`AGENTS.md`, `roadmap/README.md`, `docs/style_ada.md`, `docs/style_go.md`, `tools/coverage_thresholds.toml`, `tools/gnatcheck.rules`, `.golangci.yml`, `docs/adr/0000-...`, `docs/adr/template.md`), so a reader who follows a link lands on the canonical source and future drift is visible at the link, not silently in this file.
  - **Per-task contract** is reproduced **verbatim** from `roadmap/README.md` (the four-line `- [ ] ** ... ** / *Files:* / *Verify:* / *Done when:*` shape) rather than paraphrased. Paraphrase would drift; verbatim survives roadmap edits because the next agent comparing the two will notice and either fix one or the other.
  - **Coverage rule** ships as a four-row threshold table mirroring `tools/coverage_thresholds.toml`'s `[[threshold]]` entries (runtime/ 100%, compiler/internal/emit/ 95%, compiler/internal/translate/ 95%, compiler/ 90%) with the *overlapping by design* note carried over from the toml file's header comment. The "fix is more tests, not a threshold edit" line is escalated to "threshold edits require an ADR" — promotes a soft policy to a hard one, anchored in ADR-0000's *load-bearing decisions need ADRs* heuristic.
  - **Commit format** carries the **no Claude/Anthropic co-author** rule explicitly. Source: the user's global instructions (`~/.claude/CLAUDE.md`). Promoting it to project policy means it survives any change to global instructions and is visible to outside contributors who don't read those instructions. The rule covers both commit trailers and PR descriptions; "Generated with Claude Code" and equivalents are explicitly forbidden.
  - **ADRs section** uses ADR-0000's "imagine someone six months from now writing 'why on earth did they pick X over Y'" heuristic verbatim — reusing the exact behavioral test rather than reformulating it. Routine implementation choices are explicitly out of scope ("those go in code review") so the bar for opening an ADR stays well-calibrated.
  - **Where to look when stuck** closes the doc with a one-bullet-per-artefact router (conceptual → `AGENTS.md`/`README.md`; what's next → roadmap; why → `docs/adr/`; how is this enforced → lint configs + thresholds; recent narrative → `docs/journal/`). The closing line — *"a blocked contributor is a project bug; tell us so we can fix it"* — is intentional culture-setting: it preempts the silent-blocker failure mode endemic to multi-agent projects.
  - **Verification (2026-05-01):**
    - `wc -l CONTRIBUTING.md` → **226** (≥ 60 ✓).
    - `grep -E 'AGENTS\.md|roadmap/README\.md' CONTRIBUTING.md` → **8 lines** (every section that should reference one of the two anchors does).
    - `grep -cE 'make (bootstrap|ci)'` → 5 (Quickstart code block + per-task-contract step + PR-workflow gate references).
    - ADR-0000 linked once; both style guides linked twice each; `coverage_thresholds.toml` linked twice.
    - `./tools/check_roadmap_consistency.sh` → exit 0.
    - `make ci` from a clean repo → exit 0 with `coverage gate: PASSED` (compiler/ 100.00%, runtime/ 100.00%) and `OK: roadmap status consistent across index and 12 phase file(s).`
  - **Roadmap impact:** the **CONTRIBUTING quickstart** item in `roadmap/00-foundation.md` is now ticked — last open Phase 0 documentation item. Phase 0 has **zero `- [ ]` items remaining**; the next playbook task is the *Flip Phase 0 to `DONE`* exit-criterion check. No further code changes needed before that ritual; the next agent's job is purely the status-flip + journal entry.

- [x] Flip Phase 0 to `DONE` and run the full exit-criterion check. This is the *only* gate for finishing roadmap Phase 0:
  - Confirm every `- [ ]` in `roadmap/00-foundation.md` is now `- [x]`. If any are still open, list them and stop — do not flip the status.
  - Run `make ci` from a clean tree (`make clean && make ci`). It must exit 0. Capture full output to `.maestro/playbooks/Initiation/Working/phase-03-exit-criterion.log`.
  - Edit `roadmap/00-foundation.md`: change `Status: IN_PROGRESS` to `Status: DONE`.
  - Edit `roadmap/01-minimal-transpiler.md`: change `Status: NOT_STARTED` to `Status: IN_PROGRESS` (Phase 0's prerequisites for Phase 1 are now satisfied).
  - Edit `roadmap/README.md` phase table: flip Phase 0 row to `DONE`, Phase 1 row to `IN_PROGRESS`.
  - Run `./tools/check_roadmap_consistency.sh` — must exit 0. If it reports a mismatch, fix the table cell that is wrong (the script's output points exactly at it).

  **Outcome (2026-05-01):**
  - **Open-item audit:** all 15 `- [ ]` items in `roadmap/00-foundation.md` are now `- [x]`. `grep -c '^- \[ \]' roadmap/00-foundation.md` = **0**; `grep -c '^- \[x\]' roadmap/00-foundation.md` = **15**. No item left dangling — the prior four playbook tasks (ADR scaffolding, four ADRs, two style guides, CONTRIBUTING) closed out the last open Phase 0 documentation work, and the prerequisites for the flip are satisfied.
  - **Clean-tree exit-criterion run** — `make clean && make ci` from `/Users/ikari/src/gada` exits **0** with the full pipeline green: lint clean (Go side `0 issues.`, Ada side documented soft-skip on macOS), `go test -race` green across 3 packages (`cmd/gada` 1.433s, `internal/ping` 1.588s, `internal/version` 1.277s), AUnit harness green (`Total Tests Run: 1, Successful Tests: 1, Failed Assertions: 0`), Go coverage `total: (statements) 100.0%`, lcov `100.0% (3 of 3 lines)`, coverage gate `PASS: 'runtime/' 100.00%` + `PASS: 'compiler/' 100.00%` (and the two Phase-1-pending prefixes `compiler/internal/emit/` + `compiler/internal/translate/` correctly `SKIP` rather than `FAIL` on the empty-set), and the consistency check `OK: roadmap status consistent across index and 12 phase file(s).` Full output captured to `.maestro/playbooks/Initiation/Working/phase-03-exit-criterion.log`.
  - **Three status flips, in order, atomic per file:**
    1. `roadmap/00-foundation.md` line 5: `Status: IN_PROGRESS` → `Status: DONE`.
    2. `roadmap/01-minimal-transpiler.md` line 5: `Status: NOT_STARTED` → `Status: IN_PROGRESS`. (Phase 1's `Prerequisites:` line was already `Phase 0 DONE` — that line was authored when the phase file was first written and predicted this exact transition; no edit needed there.)
    3. `roadmap/README.md` phase-table rows for Phase 0 and Phase 1 flipped in lockstep — Phase 0 cell `IN_PROGRESS` → `DONE`, Phase 1 cell `NOT_STARTED` → `IN_PROGRESS`. The two table edits were done as a single `Edit` call so the consistency check could not see a transient single-side flip.
  - **Post-flip consistency check** — `./tools/check_roadmap_consistency.sh` exits **0** with `OK: roadmap status consistent across index and 12 phase file(s).` All three sources of truth (the two phase files' `Status:` lines + the index table) now agree.
  - **Roadmap impact:** **Phase 0 is DONE.** The active phase is now Phase 1 (Minimal transpiler). The next playbook task — the phase-completion journal entry — has authoritative material to draw on (the captured exit-criterion log, the four ADRs landed under this phase, and the seven Phase-0 items that flipped during this phase's first three playbook tasks: ADR-0000 + four ADRs + style_ada + style_go + CONTRIBUTING). No further code or roadmap edits are required before that journal entry; Phase 1's first item (*Define the GADA-IR*) is the next executable work after the journal lands.

- [x] Capture a phase-completion journal entry. This is the durable record of what shipped in roadmap Phase 0:
  - Create `docs/journal/2026-04-30-phase00-done.md` with YAML front matter (`type: note`, `tags: [milestone, phase-0, foundation]`, `related: ["[[ADR-0001]]", "[[ADR-0002]]", "[[ADR-0003]]", "[[ADR-0004]]", "[[roadmap/00-foundation]]"]`).
  - Body: bullet list of every roadmap-0 item completed with a one-line outcome each, the `make ci` exit code from the captured log, the four ADRs created (each with a one-sentence summary), and a link to the Phase 1 entry point. Keep it under 100 lines.

  **Outcome (2026-05-01):**
  - Wrote `docs/journal/2026-04-30-phase00-done.md` (**97 lines**, ≤ 100 ✓). Front matter matches the spec verbatim (`type: note`, `tags: [milestone, phase-0, foundation]`, `related:` array of the four ADRs + `[[roadmap/00-foundation]]`); `title` and `created: 2026-05-01` added to mirror the structured-Markdown convention used by [[2026-04-30-phase01-bootstrap]].
  - Body order: opening summary paragraph (15 items ticked + status flip + pointer to where the per-item rationale and narrative trail live, so the journal stays terse rather than re-summarising 426 lines of `roadmap/00-foundation.md` Notes blocks) → numbered list of all 15 roadmap items with one-line outcomes → exit-criterion run (`make clean && make ci` → exit **0** with `coverage gate: PASSED` and `OK: roadmap status consistent`, captured-log path cited) → four ADRs each with one-sentence summary + the 0001↔0002 / 0002→0003 / 0002→0004 / 0003↔0004 cross-link graph → status flips + Phase 1 entry point (**Define the GADA-IR**, `compiler/internal/ir/ir.go`).
  - Wiki-link discipline: every cross-reference uses `[[Document-Name]]` syntax — four ADRs, the roadmap phase file, the prior bootstrap journal, and the playbook itself. This closes the graph that ADR-0000 mandated and that DocGraph/Obsidian renders. The journal is an *index* into the durable artefacts, not a re-statement of them.
  - Verification: `wc -l` = 97 (≤ 100 ✓); `grep -cE 'ADR-000[1-4]'` = 8 (all four ADRs referenced in both `related:` and body ✓); `grep -cE 'roadmap/01-minimal-transpiler'` = 3 (Phase 1 entry point linked ✓); `./tools/check_roadmap_consistency.sh` = exit 0.
  - Roadmap impact: this is the last task in [[Phase-03-ADRs-Style-Contributing]]. With the journal landed, Phase 0 is fully closed in both the source-of-truth roadmap files and the durable narrative trail, and the next playbook in the Initiation series can begin Phase 1 work.
